---
phase: 03-pr-294-remediation-eran-s-review-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml
autonomous: true
requirements:
  - CONS-01

must_haves:
  truths:
    - "Both Windows and Linux VM specs include domain.memory.guest"
    - "Both Windows and Linux VM specs include domain.resources.requests.memory"
  artifacts:
    - path: "collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml"
      provides: "Consistent memory configuration across OS families"
      contains: "domain.memory.guest"
  key_links:
    - from: "create_build_spec.yaml Windows branch"
      to: "vm_memory variable"
      via: "domain.memory.guest and domain.resources.requests.memory"
      pattern: "domain\\.memory\\.guest.*vm_memory"
    - from: "create_build_spec.yaml Linux branch"
      to: "vm_memory variable"
      via: "domain.memory.guest and domain.resources.requests.memory"
      pattern: "domain\\.memory\\.guest.*vm_memory"
---

<objective>
Verify and document that both Linux and Windows VM spec blocks consistently specify guest memory and scheduling requests.

Purpose: Address CONS-01 from @eranco74's review. Research indicates commit 148c064 may have already fixed this, but verification is needed to ensure both branches have both fields.

Output: Verified consistency or fix applied if missing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03/03-RESEARCH.md
@collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit memory fields in both OS family branches</name>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml</files>
  <action>
    Read create_build_spec.yaml and verify both conditional branches have both memory fields.

    Required fields in BOTH branches:
    1. domain.memory.guest: "{{ vm_memory }}"  # Guest-visible memory
    2. domain.resources.requests.memory: "{{ vm_memory }}"  # Kubernetes scheduling request

    Check Linux branch (when: guest_os_family != 'windows'):
    - grep for "domain.memory.guest" in context of Linux conditional
    - grep for "domain.resources.requests.memory" in context of Linux conditional

    Check Windows branch (when: guest_os_family == 'windows'):
    - grep for "domain.memory.guest" in context of Windows conditional
    - grep for "domain.resources.requests.memory" in context of Windows conditional

    If BOTH branches have BOTH fields → verification complete, document in commit message.
    If ANY branch is missing EITHER field → fix it.

    Research indicates this was likely fixed in commit 148c064, but we must verify by reading the current file.
  </action>
  <verify>
    <automated>
      # Extract both branches and verify each has both fields
      # Linux branch check
      awk '/when: guest_os_family != .windows./,/when: guest_os_family == .windows./' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml | grep -q "domain.memory.guest" &amp;&amp; \
      awk '/when: guest_os_family != .windows./,/when: guest_os_family == .windows./' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml | grep -q "domain.resources.requests.memory" &amp;&amp; \
      # Windows branch check
      awk '/when: guest_os_family == .windows./,/^$/' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml | grep -q "domain.memory.guest" &amp;&amp; \
      awk '/when: guest_os_family == .windows./,/^$/' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml | grep -q "domain.resources.requests.memory"
    </automated>
  </verify>
  <done>Both Linux and Windows branches have domain.memory.guest AND domain.resources.requests.memory</done>
</task>

<task type="auto">
  <name>Task 2: Fix missing fields if found (conditional)</name>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml</files>
  <action>
    IF Task 1 verification failed (any branch missing any field), apply fixes:

    Linux branch needs both:
    ```yaml
    domain:
      memory:
        guest: "{{ vm_memory }}"
      resources:
        requests:
          memory: "{{ vm_memory }}"
    ```

    Windows branch needs both:
    ```yaml
    domain:
      memory:
        guest: "{{ vm_memory }}"
      resources:
        requests:
          memory: "{{ vm_memory }}"
    ```

    If Task 1 verification passed, skip this task (document "already compliant" in commit).

    Preserve all other Windows-specific fields (firmware.bootloader.efi, features.hyperv, clock settings, etc).
  </action>
  <verify>
    <automated>
      # Same verification as Task 1
      awk '/when: guest_os_family != .windows./,/when: guest_os_family == .windows./' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml | grep -q "domain.memory.guest" &amp;&amp; \
      awk '/when: guest_os_family != .windows./,/when: guest_os_family == .windows./' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml | grep -q "domain.resources.requests.memory" &amp;&amp; \
      awk '/when: guest_os_family == .windows./,/^$/' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml | grep -q "domain.memory.guest" &amp;&amp; \
      awk '/when: guest_os_family == .windows./,/^$/' collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml | grep -q "domain.resources.requests.memory"
    </automated>
  </verify>
  <done>All missing fields added, or verified already compliant</done>
</task>

<task type="auto">
  <name>Task 3: Syntax check and commit</name>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml</files>
  <action>
    Run ansible-lint to validate changes.

    Command:
    ansible-lint collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml

    If changes were made (Task 2 applied fixes):
    git add collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml
    git commit -s -m "fix(ocp_virt_vm): ensure consistent memory fields across OS families

    - Add domain.memory.guest to [Linux|Windows] branch
    - Add domain.resources.requests.memory to [Linux|Windows] branch

    Both guest-visible memory and Kubernetes scheduling requests now specified
    consistently across all OS families.

    Resolves CONS-01 from PR #294 review.

    Signed-off-by: $(git config user.name) &lt;$(git config user.email)&gt;
    Assisted-by: Claude Code &lt;noreply@anthropic.com&gt;"

    If no changes needed (verification passed in Task 1):
    git commit --allow-empty -s -m "docs(ocp_virt_vm): verify memory field consistency (CONS-01)

    Verified both Linux and Windows VM spec blocks include:
    - domain.memory.guest (guest-visible memory)
    - domain.resources.requests.memory (K8s scheduling request)

    No changes required — already compliant.

    Resolves CONS-01 from PR #294 review.

    Signed-off-by: $(git config user.name) &lt;$(git config user.email)&gt;
    Assisted-by: Claude Code &lt;noreply@anthropic.com&gt;"
  </action>
  <verify>
    <automated>ansible-lint collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml</automated>
  </verify>
  <done>Lint passes, changes or verification committed with descriptive message</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role→KubeVirt API | VM spec submitted to KubeVirt for scheduling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | inconsistent memory config causes VM scheduling failure | mitigate | Ensure both guest and request memory fields present (CONS-01) |
</threat_model>

<verification>
After all tasks complete:
1. Read both branches of create_build_spec.yaml
2. Verify Linux branch has domain.memory.guest and domain.resources.requests.memory
3. Verify Windows branch has domain.memory.guest and domain.resources.requests.memory
4. Run ansible-lint — should pass
</verification>

<success_criteria>
1. Linux VM spec includes domain.memory.guest
2. Linux VM spec includes domain.resources.requests.memory
3. Windows VM spec includes domain.memory.guest
4. Windows VM spec includes domain.resources.requests.memory
5. ansible-lint passes
6. Changes or verification committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/03/03-03-SUMMARY.md`
</output>
