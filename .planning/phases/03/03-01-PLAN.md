---
phase: 03-pr-294-remediation-eran-s-review-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml
  - collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates/unattend.xml.j2
autonomous: true
requirements:
  - SEC-01
  - MAINT-01

must_haves:
  truths:
    - "No ConfigMap resources contain sensitive unattend.xml or passwords"
    - "unattend.xml is stored as Kubernetes Secret"
    - "create_secrets.yaml contains no inline XML"
  artifacts:
    - path: "collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates/unattend.xml.j2"
      provides: "Sysprep XML template with Jinja2 variables"
      min_lines: 50
    - path: "collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml"
      provides: "Secret creation using template lookup"
      contains: "kind: Secret"
  key_links:
    - from: "collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml"
      to: "templates/unattend.xml.j2"
      via: "lookup('template', 'unattend.xml.j2')"
      pattern: "lookup\\('template'"
    - from: "collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml"
      to: "kubernetes Secret"
      via: "kind: Secret with stringData"
      pattern: "kind: Secret"
---

<objective>
Migrate sysprep unattend.xml storage from ConfigMap to Secret and extract inline XML to a Jinja2 template.

Purpose: Address security and maintainability issues (SEC-01, MAINT-01) identified in @eranco74's review. ConfigMaps expose sensitive data to namespace readers and are not encrypted at rest. Inline XML (~50 lines) makes the task file hard to read and diff.

Output: Secret-based sysprep configuration with template-based XML rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03/03-RESEARCH.md
@collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml
@collections/ansible_collections/osac/templates/roles/ocp_virt_vm/defaults/main.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract unattend.xml to Jinja2 template</name>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates/unattend.xml.j2</files>
  <action>
    Create templates/ directory in role and extract inline XML from create_secrets.yaml lines 19-90 to unattend.xml.j2.

    Template must:
    - Preserve all Jinja2 variable substitutions (vm_sysprep_*)
    - Include XML declaration: <?xml version="1.0" encoding="utf-8"?>
    - Match exact structure from current ConfigMap data (oobeSystem + specialize passes)
    - Use same locale/password/timezone variables as current inline version

    Directory creation: mkdir -p collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates

    Template content sourced from lines 20-90 of create_secrets.yaml (the Unattend.xml: | block).
  </action>
  <verify>
    <automated>
      test -f collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates/unattend.xml.j2 &amp;&amp; \
      grep -q "vm_sysprep_admin_password" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates/unattend.xml.j2 &amp;&amp; \
      grep -q "Microsoft-Windows-Shell-Setup" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates/unattend.xml.j2
    </automated>
  </verify>
  <done>Template file exists with complete unattend.xml structure including all vm_sysprep_* variables</done>
</task>

<task type="auto">
  <name>Task 2: Refactor create_secrets.yaml to use Secret + template lookup</name>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml</files>
  <action>
    Replace ConfigMap with Secret in create_secrets.yaml (task starting at line 7).

    Changes:
    1. Change kind: ConfigMap → kind: Secret
    2. Add type: Opaque under kind
    3. Replace data: with stringData: (Kubernetes auto-encodes to base64)
    4. Replace inline XML block (lines 19-90) with template lookup:
       Unattend.xml: "{{ lookup('template', 'unattend.xml.j2') }}"

    Also update volume reference in vm_template_spec (around line 96-99):
    - Change configMap: → secret:
    - Change name: → secretName:

    Preserve:
    - All conditionals (when: guest_os_family == 'windows', vm_enable_sysprep | bool)
    - kubeconfig handling (remote_cluster_kubeconfig | default(omit))
    - metadata.labels (default_vm_labels)
    - Resource naming pattern (compute_instance_name-sysprep)

    Why not base64encode filter: stringData is simpler and Kubernetes handles encoding.
  </action>
  <verify>
    <automated>
      grep -q "kind: Secret" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml &amp;&amp; \
      grep -q "type: Opaque" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml &amp;&amp; \
      grep -q "stringData:" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml &amp;&amp; \
      grep -q "lookup('template', 'unattend.xml.j2')" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml &amp;&amp; \
      ! grep -q "kind: ConfigMap" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml
    </automated>
  </verify>
  <done>create_secrets.yaml uses Secret with stringData, references template file, no inline XML or ConfigMap</done>
</task>

<task type="auto">
  <name>Task 3: Update sysprep volume mount to use Secret</name>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml</files>
  <action>
    Locate the vm_template_spec volume definition for sysprep-disk (around lines 96-99 per research).

    Change from:
    ```yaml
    volumes:
      - name: sysprep-disk
        sysprep:
          configMap:
            name: "{{ compute_instance_name }}-sysprep"
    ```

    To:
    ```yaml
    volumes:
      - name: sysprep-disk
        sysprep:
          secret:
            secretName: "{{ compute_instance_name }}-sysprep"
    ```

    This ensures the VirtualMachine can mount the Secret instead of the now-deleted ConfigMap.
  </action>
  <verify>
    <automated>
      grep -A 3 "name: sysprep-disk" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml | grep -q "secret:" &amp;&amp; \
      grep -A 4 "name: sysprep-disk" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml | grep -q "secretName:" &amp;&amp; \
      ! grep -A 3 "name: sysprep-disk" collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml | grep -q "configMap:"
    </automated>
  </verify>
  <done>Volume mount references secret.secretName instead of configMap.name</done>
</task>

<task type="auto">
  <name>Task 4: Syntax check and commit</name>
  <files>collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml, collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates/unattend.xml.j2</files>
  <action>
    Run ansible-lint and syntax check to validate changes.

    Commands:
    1. ansible-lint collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml
    2. ansible-playbook --syntax-check playbook_osac_create_compute_instance.yml

    If lint passes, commit:
    git add collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml \
            collections/ansible_collections/osac/templates/roles/ocp_virt_vm/templates/unattend.xml.j2
    git commit -s -m "fix(ocp_virt_vm): migrate sysprep config from ConfigMap to Secret

    - Extract unattend.xml inline XML to templates/unattend.xml.j2
    - Change sysprep resource from ConfigMap to Secret (type: Opaque)
    - Use stringData + template lookup for XML rendering
    - Update volume mount to reference secret.secretName

    Resolves SEC-01, MAINT-01 from PR #294 review.

    Signed-off-by: $(git config user.name) &lt;$(git config user.email)&gt;
    Assisted-by: Claude Code &lt;noreply@anthropic.com&gt;"
  </action>
  <verify>
    <automated>ansible-lint collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_secrets.yaml</automated>
  </verify>
  <done>Lint passes, changes committed with descriptive message</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Ansible controller submits Secret to Kubernetes API |
| API→etcd | Kubernetes stores Secret (encrypted at rest if cluster configured) |
| namespace→RBAC | Secret readable only by users with Secret read permissions in namespace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | sysprep ConfigMap | mitigate | Change to Secret with type: Opaque (SEC-01) |
| T-03-02 | Information Disclosure | plaintext password in git | accept | Password is variable substitution, not hardcoded value |
| T-03-03 | Tampering | template injection in XML | mitigate | Jinja2 auto-escaping enabled by default in Ansible |
| T-03-04 | Elevation of Privilege | namespace privilege escalation | accept | RBAC enforced by cluster, out of scope for role |
</threat_model>

<verification>
After all tasks complete:
1. grep for "kind: ConfigMap" in create_secrets.yaml → should return 0 results
2. grep for "kind: Secret" in create_secrets.yaml → should return 1+ results
3. wc -l on unattend.xml.j2 → should be ~50-70 lines
4. ansible-lint should pass with no errors
</verification>

<success_criteria>
1. templates/unattend.xml.j2 exists with complete XML structure
2. create_secrets.yaml creates Secret (not ConfigMap)
3. create_secrets.yaml contains no inline XML (uses template lookup)
4. Volume mount references secret.secretName
5. ansible-lint passes
6. Changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/03/03-01-SUMMARY.md`
</output>
