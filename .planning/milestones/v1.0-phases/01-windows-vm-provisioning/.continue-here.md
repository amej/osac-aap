---
context: phase
phase: 01-windows-vm-provisioning
task: 0
total_tasks: 0
status: paused
last_updated: "2026-05-12T04:23:11Z"
---

<current_state>
Milestone v1.1 is shipped. This session performed E2E validation of a Windows 11 VM
with sysprep on the SNO cluster (api.my.sno.internal:6443) and applied a code review
fix to create_build_spec.yaml for memory field consistency.

The VM (demo-windows-vm) is currently Running in the demo-windows namespace on the
SNO cluster. The test playbook (test-windows-golden-image.yml) was temporarily modified
to enable sysprep — it needs reverting before any commit.
</current_state>

<completed_work>

Completed Tasks:
- E2E test: Windows 11 VM with sysprep — Done. VM reached Running on SNO cluster.
  Sysprep ConfigMap created with Unattend.xml, sysprep disk attached as SATA CD-ROM.
  First run failed (cluster resource exhaustion, 904s timeout). Second run succeeded
  after user freed resources.
- Fix create_build_spec.yaml memory consistency — Done. Added domain.memory.guest to
  Windows spec and domain.resources.requests.memory to Linux spec. ansible-lint passed
  at production profile. No downstream files need updates.
</completed_work>

<remaining_work>

- Revert test-windows-golden-image.yml to golden image defaults (vm_enable_sysprep: false,
  assert sysprep disk NOT present) — the file was temporarily modified for this session's
  E2E test
- Commit create_build_spec.yaml memory consistency fix
- Sync run-windows-vm-pr branch changes to osac-windows-vm-support-codeonly branch
  (user requested, not yet started — branches have diverged with ~20 commits each way)
- Decide what to do with vendor/ updates and ocp_4_17_small_github argument_specs.yaml
  (uncommitted, pre-existing from before this session)
</remaining_work>

<decisions_made>

- Added both domain.memory.guest and domain.resources.requests.memory to both Linux and
  Windows VM template specs for consistency (Linux had only guest, Windows had only requests)
</decisions_made>

<blockers>
- None
</blockers>

## Required Reading (in order)
1. `collections/ansible_collections/osac/templates/roles/ocp_virt_vm/tasks/create_build_spec.yaml` — the memory consistency fix
2. `tests/test-windows-golden-image.yml` — currently modified for sysprep testing, needs revert

## Infrastructure State
- SNO cluster (api.my.sno.internal:6443): demo-windows-vm Running in demo-windows namespace
- demo-windows-vm-sysprep ConfigMap exists in demo-windows namespace
- demo-windows-vm-root-disk DataVolume (80Gi, lvms-vg1) exists in demo-windows namespace

<context>
Session was focused on validation, not new feature work. The user wanted to run an E2E
test proving the Windows VM + sysprep path works on a real cluster, then apply a code
review finding. Both completed successfully. The branch sync task was interrupted by the
user before starting — they may want to revisit the approach (merge vs reset) given the
two branches have diverged significantly.
</context>

<next_action>
Start with: Revert test-windows-golden-image.yml to golden image defaults, then commit
the create_build_spec.yaml fix. After that, address the branch sync to
osac-windows-vm-support-codeonly.
</next_action>
